extends layout

block content
  h1.mt-3='💪 FAB Check In 🥦'

  form(method='POST' action='')
    div.form-group
      label(for='user') Name:
      input#user.form-control(type='text', placeholder='Just your first name..' name='user' required='true' value=user )
    div.form-group
      label(for='weight') Weight (If you feel like sharing):
      input#weight.form-control(type='text', placeholder='Number, or "Down a tenth of a pound since yesterday!" etc..' name='weight' value=weight )
      label(for='activity') Activity (Make sure you're at least MOVING! 👟): 
      input#activity.form-control(type='text', placeholder='Step Count, or "Went on a walk today" etc..' name='activity' value=activity )
    div.form-group
      label(for='update') Update (Anything else you feel like sharing):
      input#update.form-control(type='text', placeholder='e.g. Just wanted to note how handsome Scott is..', name='update' value=update )
    button.btn.btn-primary(type='submit') Submit
    div.form-group.d-none
      label(for='password') Password: 
      input#password.form-control(type='text', name='password' value=parameterPW )
  
  br
  if errors 
    h3.text-danger='Error:'
    ul
      for error in errors
        li.text-danger!= error.msg